#The function simple_interest(p, t, r) is called with the keyword arguments the order of arguments doesn't matter in this case  



# formulae

# Simple Interest = (principal) * (rate) * (# of periods)


def simple_interst(a,b,c):
    return (a*b*c)/100

print(simple_interst(a=10,b=30,c=40))